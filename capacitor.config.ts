import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'tarea5_ionic',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
